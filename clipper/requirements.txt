# Python 3.12 (3.11 also works). ffmpeg + ffprobe must be installed separately.
yt-dlp>=2024.8.6
gdown>=5.2.0
faster-whisper>=1.0.3
groq>=0.11.0
numpy>=1.26
Pillow>=10.0
# Document extraction for intake (optional — intake degrades gracefully if missing)
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
