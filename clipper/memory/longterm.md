# LONGTERM MEMORY — permanent

Read fully at the start of every session. Grows forever. Campaign-specific rules
NEVER live here (a rule for campaign X must not censor campaign Y) — only durable,
cross-campaign lessons. See instructions.md → MEMORY SYSTEM.

## Style Rules (permanent, cross-campaign)
_Derived from repeated kill reasons. Each becomes a hard rule once a theme repeats._
- (seed) The moment IS the video — raw, minimal cutting, 10–40s.
- (seed) First frame must be arresting (mid-action / face / chaos), never a dead frame.
- (seed) One top caption line; casual, lowercase energy, emoji as punctuation.

## Kill Lessons
_Every review kill (weak hook / boring / bad moment / off-style / rules risk / custom)
gets logged here with the clip + reason. Repeated themes graduate to Style Rules._
- (none yet)

## Caption Patterns
### Won
- (seed) `stakes + outcome + emotion` — e.g. "bro lost the whole run in 10 seconds 😭✌️"
### Lost
- (seed) Formal phrasing / "X reacts to Y" — never use.

## Performance History
_Per-clip 48h view counts pasted by the user. Clips crossing threshold → Golden Library._
- (none yet)

## Golden Library
_References to clips that overperformed (path + why it worked)._
- (none yet)

## User Preferences
- User posts and submits manually. NEVER automate posting or suggest it.
- Trending sounds are added in-app at post time — never baked into the file.
